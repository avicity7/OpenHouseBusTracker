apiVersion: v1
kind: Service
metadata:
  name: opbt-db
  labels:
    app: opbt-db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgresql
  selector:
    app: opbt-db
