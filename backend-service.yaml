apiVersion: v1
kind: Service
metadata:
  name: opbt-backend
  labels:
    app: opbt-backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: opbt-backend
